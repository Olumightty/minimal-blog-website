<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Email - Minimal Blog</title>
    <link rel="stylesheet" href="/stylesheets/validate-email.css">
</head>
<body>
    <main class="container">
        <div class="logo-container">
            <h1><a href="index.html">Minimal Blog</a></h1>
            <p class="tagline">A clean space for thoughts and ideas</p>
        </div>
        
        <div class="verification-container">
            <div class="verification-header">
                <h2>Verify Your Email</h2>
                <p>We've sent a 4-digit code to your email address. Please enter it below to verify your account.</p>
            </div>

            <form id="verification-form" class="verification-form">
                <div class="otp-container">
                    <input type="text" class="otp-input" maxlength="1" autofocus>
                    <input type="text" class="otp-input" maxlength="1">
                    <input type="text" class="otp-input" maxlength="1">
                    <input type="text" class="otp-input" maxlength="1">
                </div>
                
                <p class="error-message" id="otp-error"></p>

                <div class="timer-container">
                    <p>Code expires in: <% if(timeLeft){ %><span id="timer"><%= timeLeft%> </span> <%}%></p>
                </div>

                <button type="submit" class="verify-button">Verify Email</button>
                
                <div class="resend-container">
                    <p>Didn't receive the code? <button type="button" id="resend-button" class="resend-button">Resend</button></p>
                </div>
            </form>

            <div class="help-text">
                <p>If you're having trouble, please <a href="/contact">contact us</a> for assistance.</p>
            </div>
        </div>
    </main>
    <%- include('partials/toaster') %>
    <script src="/javascripts/toaster.js"></script>
    <script src="/javascripts/validate-email.js"></script>
</body>
</html>