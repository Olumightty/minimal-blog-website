<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Email - Minimal Blog</title>
    <link rel="stylesheet" href="/stylesheets/validate-email.css">
</head>
<body>
    <div class="background-pattern"></div>
    
    <main class="container">
        <div class="verification-card">
            <!-- Email Icon -->
            <div class="icon-container">
                <div class="email-icon">
                    <svg width="48" height="48" viewBox="0 0 24 24" fill="none">
                        <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        <polyline points="22,6 12,13 2,6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                </div>
            </div>

            <!-- Header -->
            <div class="verification-header">
                <h1>Check Your Email</h1>
                <p class="subtitle">We've sent a 4-digit verification code to your email address</p>
            </div>

            <!-- Form -->
            <form id="verification-form" class="verification-form">
                <!-- OTP Input Container -->
                <div class="otp-section">
                    <label class="otp-label">Enter verification code</label>
                    <div class="otp-container">
                        <input type="text" class="otp-input" maxlength="1" autofocus aria-label="First digit">
                        <input type="text" class="otp-input" maxlength="1" aria-label="Second digit">
                        <input type="text" class="otp-input" maxlength="1" aria-label="Third digit">
                        <input type="text" class="otp-input" maxlength="1" aria-label="Fourth digit">
                    </div>
                    <div class="error-message" id="otp-error" role="alert"></div>
                </div>

                <!-- Timer -->
                <div class="timer-section">
                    <div class="timer-container">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" class="timer-icon">
                            <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2"/>
                            <polyline points="12,6 12,12 16,14" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>Code expires in: <% if(timeLeft){ %><span id="timer" class="timer-text"><%= timeLeft%> </span> <%}%></span>
                    </div>
                </div>

                <!-- Submit Button -->
                <button type="submit" class="verify-button" disabled>
                    <span class="button-text">Verify Email</span>
                    <div class="button-loader" style="display: none;">
                        <div class="spinner"></div>
                    </div>
                </button>
                
                <!-- Resend Section -->
                <div class="resend-section">
                    <p class="resend-text">Didn't receive the code?</p>
                    <button type="button" id="resend-button" class="resend-button">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" class="resend-icon">
                            <polyline points="23,4 23,10 17,10" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                            <path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                        <span>Resend Code</span>
                    </button>
                </div>
            </form>

            <!-- Help Text -->
            <div class="help-section">
                <p>Having trouble? <a href="/contact" class="help-link">Contact support</a></p>
            </div>
        </div>
    </main>

    <!-- Toast Container -->
    <div id="toast-container"></div>

    <script src="/javascripts/validate-email.js"></script>
</body>
</html>